# Environment Configuration for Library Management System Frontend
# Do NOT put secret keys or passwords in this file (Vite exposes VITE_ variables to the browser).

VITE_API_BASE_URL=http://localhost:8080
